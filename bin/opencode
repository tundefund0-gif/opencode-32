#!/bin/sh
SCR="$(cd "$(dirname "$0")" && pwd)"
if [ -L "$0" ]; then
  TGT="$(readlink "$0")"
  case "$TGT" in /*) SCR="$(dirname "$TGT")" ;; *) SCR="$(cd "$(dirname "$0")/$TGT" && pwd)" ;; esac
fi
exec node "$SCR/opencode.js" "$@"
